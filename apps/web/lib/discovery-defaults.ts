export const DEFAULT_DISCOVERY_SETTINGS = {
  adaptive_discovery_enabled: true,
  daily_discovery_target: 25,
  daily_discovery_candidate_cap: 40,
  daily_discovery_attempt_cap: 80,
  daily_discovery_cost_cap_usd: 0.7,
} as const
